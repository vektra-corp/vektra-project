import { SectionSkeleton } from '@/components/layout/skeletons'

/** Project views: the project layout supplies the Topbar and view tabs. */
export default function Loading() {
  return <SectionSkeleton />
}
