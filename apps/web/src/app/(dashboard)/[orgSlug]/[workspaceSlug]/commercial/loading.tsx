import { SectionSkeleton } from '@/components/layout/skeletons'

/** Commercial: the section layout supplies the Topbar. */
export default function Loading() {
  return <SectionSkeleton variant="table" />
}
