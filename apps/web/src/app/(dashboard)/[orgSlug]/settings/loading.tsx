import { SectionSkeleton } from '@/components/layout/skeletons'

/** Settings: the section layout supplies the Topbar, so only the body is replaced. */
export default function Loading() {
  return <SectionSkeleton />
}
